1. Make directory
    mkdir git_thir
    cd git_thir
2. Initialize a git repo in the folder and run:
    git init  
3. To add a new file to the repo:
    touch howto_git
4. Use git status to see the files
5. To add a file  to a commit, you first ned to add it to the stagging environment
    git add howto_git
6. Create a commit:
   git commit -m "Thir's first commit"
   git comitt -a
